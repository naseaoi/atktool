{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ATK Battery desktop windows",
  "windows": ["overlay", "manager"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "allow-local-commands"
  ]
}
